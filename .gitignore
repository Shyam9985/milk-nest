node_modules/
.env

logs/
metrics/
coverage/
dist/

# user-uploaded files live on disk, not in git
server/uploads/

public/
# server/public is build output, but the clients' public folders are source: they hold the
# index.html, favicon, manifest and images the apps are served with
!client/public-website/public/
!client/web-admin/public/

*.log

.vscode/settings.json
certs/