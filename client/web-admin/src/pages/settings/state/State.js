import UnderDevelopment from '../../utils/UnderDevelopment';

function State() {
    return (
        <UnderDevelopment
            title="State"
            subtitle="State master is being prepared"
            description="This section will help manage state entries and related configuration soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default State;
