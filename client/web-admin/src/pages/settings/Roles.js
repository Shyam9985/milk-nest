import UnderDevelopment from '../../utils/UnderDevelopment';

function Roles() {
    return (
        <UnderDevelopment
            title="Roles"
            subtitle="Role management is being prepared"
            description="This section will handle role definitions and role-based setup soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Roles;
