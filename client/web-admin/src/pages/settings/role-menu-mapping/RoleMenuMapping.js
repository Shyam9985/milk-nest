import UnderDevelopment from '../../utils/UnderDevelopment';

function RoleMenuMapping() {
    return (
        <UnderDevelopment
            title="Role Menu Mapping"
            subtitle="Mapping setup is being prepared"
            description="Role-to-menu mapping will be handled here once the feature is implemented."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default RoleMenuMapping;
