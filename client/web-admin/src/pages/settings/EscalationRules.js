import UnderDevelopment from '../../utils/UnderDevelopment';

function EscalationRules() {
    return (
        <UnderDevelopment
            title="Escalation Rules"
            subtitle="Escalation rules are being prepared"
            description="Escalation workflow rules will be configurable here soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default EscalationRules;
