import UnderDevelopment from '../../utils/UnderDevelopment';

function Organization() {
    return (
        <UnderDevelopment
            title="Organization"
            subtitle="Organization setup is being prepared"
            description="Organization-wide configuration will be available here soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Organization;
