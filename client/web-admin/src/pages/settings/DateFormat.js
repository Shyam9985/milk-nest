import UnderDevelopment from '../../utils/UnderDevelopment';

function DateFormat() {
    return (
        <UnderDevelopment
            title="Date Format"
            subtitle="Date format setup is being prepared"
            description="Date and formatting preferences will be configurable here soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default DateFormat;
