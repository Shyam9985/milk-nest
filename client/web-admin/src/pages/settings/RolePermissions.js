import UnderDevelopment from '../../utils/UnderDevelopment';

function RolePermissions() {
    return (
        <UnderDevelopment
            title="Role Permissions"
            subtitle="Permission configuration is being prepared"
            description="Role-permission assignment will be available here once the module is ready."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default RolePermissions;
