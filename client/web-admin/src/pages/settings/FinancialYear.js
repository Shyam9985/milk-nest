import UnderDevelopment from '../../utils/UnderDevelopment';

function FinancialYear() {
    return (
        <UnderDevelopment
            title="Financial Year"
            subtitle="Financial year setup is being prepared"
            description="This section will allow financial year management once the module is ready."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default FinancialYear;
