import UnderDevelopment from '../../utils/UnderDevelopment';

function Theme() {
    return (
        <UnderDevelopment
            title="Theme"
            subtitle="Theme configuration is being prepared"
            description="Theme and appearance management will be available here soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Theme;
