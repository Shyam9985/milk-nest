import UnderDevelopment from '../../utils/UnderDevelopment';

function WorkflowTypes() {
    return (
        <UnderDevelopment
            title="Workflow Types"
            subtitle="Workflow types are being prepared"
            description="Workflow type definitions will be managed here in a future update."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default WorkflowTypes;
