import UnderDevelopment from '../../utils/UnderDevelopment';

function Department() {
    return (
        <UnderDevelopment
            title="Department"
            subtitle="Department master is being prepared"
            description="Department setup will be available here once the module is implemented."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Department;
