import UnderDevelopment from '../../utils/UnderDevelopment';

function Designation() {
    return (
        <UnderDevelopment
            title="Designation"
            subtitle="Designation master is being prepared"
            description="This section will support designation records as part of the master data setup."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Designation;
