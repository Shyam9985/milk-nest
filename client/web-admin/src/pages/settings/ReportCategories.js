import UnderDevelopment from '../../utils/UnderDevelopment';

function ReportCategories() {
    return (
        <UnderDevelopment
            title="Report Categories"
            subtitle="Report categories are being prepared"
            description="Report categorization and grouping will be managed here soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default ReportCategories;
