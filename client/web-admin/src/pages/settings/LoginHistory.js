import UnderDevelopment from '../../utils/UnderDevelopment';

function LoginHistory() {
    return (
        <UnderDevelopment
            title="Login History"
            subtitle="Login history is being prepared"
            description="Audit details for user sign-ins will appear here once the module is ready."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default LoginHistory;
