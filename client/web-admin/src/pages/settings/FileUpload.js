import UnderDevelopment from '../../utils/UnderDevelopment';

function FileUpload() {
    return (
        <UnderDevelopment
            title="File Upload"
            subtitle="File upload settings are being prepared"
            description="File storage and upload configuration will be available here soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default FileUpload;
