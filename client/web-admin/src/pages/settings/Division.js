import UnderDevelopment from '../../utils/UnderDevelopment';

function Division() {
    return (
        <UnderDevelopment
            title="Division"
            subtitle="Division setup is being prepared"
            description="Division-related setup will be available here once the module is ready."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Division;
