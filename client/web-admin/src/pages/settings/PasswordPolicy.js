import UnderDevelopment from '../../utils/UnderDevelopment';

function PasswordPolicy() {
    return (
        <UnderDevelopment
            title="Password Policy"
            subtitle="Security policy setup is being prepared"
            description="Password rules and governance options will be configurable here soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default PasswordPolicy;
