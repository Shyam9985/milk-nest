import UnderDevelopment from '../../utils/UnderDevelopment';

function Language() {
    return (
        <UnderDevelopment
            title="Language"
            subtitle="Language master is being prepared"
            description="Language configuration will be available soon for localization support."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Language;
