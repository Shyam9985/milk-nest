import UnderDevelopment from '../../utils/UnderDevelopment';

function EmailTemplates() {
    return (
        <UnderDevelopment
            title="Email Templates"
            subtitle="Email templates are being prepared"
            description="Custom email templates will be managed here in a future update."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default EmailTemplates;
