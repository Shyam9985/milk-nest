import UnderDevelopment from '../../utils/UnderDevelopment';

function Position() {
    return (
        <UnderDevelopment
            title="Position"
            subtitle="Position master is being prepared"
            description="Position setup will be available here once the feature is implemented."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Position;
