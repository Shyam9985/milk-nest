import UnderDevelopment from '../../utils/UnderDevelopment';

function Region() {
    return (
        <UnderDevelopment
            title="Region"
            subtitle="Region setup is being prepared"
            description="Regional configuration details will be managed here in a future update."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Region;
