import UnderDevelopment from '../../utils/UnderDevelopment';

function SMSConfiguration() {
    return (
        <UnderDevelopment
            title="SMS Configuration"
            subtitle="SMS settings are being prepared"
            description="SMS provider and messaging configuration will be available here soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default SMSConfiguration;
