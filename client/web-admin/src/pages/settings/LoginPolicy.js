import UnderDevelopment from '../../utils/UnderDevelopment';

function LoginPolicy() {
    return (
        <UnderDevelopment
            title="Login Policy"
            subtitle="Login policy setup is being prepared"
            description="Authentication and login policy settings will be available here soon."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default LoginPolicy;
