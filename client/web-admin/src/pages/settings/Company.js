import UnderDevelopment from '../../utils/UnderDevelopment';

function Company() {
    return (
        <UnderDevelopment
            title="Company"
            subtitle="Company setup is being prepared"
            description="Company profile and setup details will be managed here in a future update."
            backRoute="/settings"
            buttonLabel="Back to Settings"
        />
    );
}

export default Company;
